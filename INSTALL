QtMediaHub requires
  * Qt >= 4.7.4
  * Qt Mobility


Building:
1. qmake (for install prefix use 'qmake PREFIX=/usr/', default PREFIX is /usr/local)
2. make
3. ./bin/qtmediahub (on mac use ./bin/qtmediahub-launcher)

